<% include inc/header %>
<div class="content-left">
	<div class="cards">
		<div class="page"> LocalPosition><a href="/article" >Article</a></div>
		<% 
			var len = articles.length; 
			for(var i = 0;i<len;i++){  
				var article = articles[i];
				if(article.type.toLowerCase().trim() !== "article" || article.show === false ) continue;
		%>
				<% include inc/article-card %>
		<%  } %>

		<div class="last"></div>
	</div>
</div>
<div class="content-right">
	<% include inc/nav-right %>
</div>
<% include inc/footer %>